dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # واجهات وتصميم
  google_fonts: ^6.1.0 # لدعم خطي (Cairo أو Amiri)
  
  # الوسائط والصوتيات
  video_player: ^2.8.2
  just_audio: ^0.9.36
  
  # شبكات ومواقيت وذكاء اصطناعي
  dio: ^5.4.0
  google_generative_ai: ^0.4.0 # محرك Gemini AI
  
  # أدوات مساعدة ونظام
  shared_preferences: ^2.2.2
  intl: ^0.19.0
  share_plus: ^7.2.2
